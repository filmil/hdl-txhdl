# Gaps between the hand-written tops and the lowered modules they
# instantiate that are known and have an issue of their own, one
# `module.port #issue` a line. //tools/topcheck:tops_test reports
# these rather than failing on them, and FAILS once one is connected
# everywhere, so the change that closes a gap deletes its line here.
#
